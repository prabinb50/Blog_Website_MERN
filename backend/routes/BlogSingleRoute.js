// import express from "express";
// import multer from "multer";
// const upload = multer({ dest: "uploads/" });
// import { createBlogSingle, deleteBlogSingleById, getAllBlogSingles, getBlogSingleById, updateBlogSingleById } from "../controller/BlogSingleController.js";

// const router = express.Router();

// router.post("/", upload.single("image"), createBlogSingle);
// router.get("/", getAllBlogSingles);
// router.get("/:id", getBlogSingleById);
// router.patch("/:id", upload.single("image"), updateBlogSingleById);
// router.delete("/:id", deleteBlogSingleById);

// export default router;